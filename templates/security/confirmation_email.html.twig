{# Email de vérification - Green Googies #}
{% set appUrl = absolute_url(path('app_products')) %}
{% set logoUrl = absolute_url(asset('images/Logo.png')) %}

<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Confirmez votre adresse e‑mail</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
        /* Styles de base compatibles emails */
        body {
            margin: 0;
            padding: 0;
            background: #f6f8fb;
        }

        .wrapper {
            width: 100%;
            background: #f6f8fb;
            padding: 24px 0;
        }

        .container {
            max-width: 640px;
            margin: 0 auto;
            background: #ffffff;
            border-radius: 8px;
            overflow: hidden;
        }

        .header {
            background: #FFFDF7;
            padding: 16px 20px;
        }

        .brand {
            display: flex;
            align-items: center;
            text-decoration: none;
        }

        .brand img {
            display: block;
            height: 18px;
            width: auto;
            vertical-align: middle;
        }

        .content {
            padding: 24px 20px;
            font-family: Arial, Helvetica, sans-serif;
            color: #1f2937;
            font-size: 15px;
            line-height: 1.6;
        }

        .title {
            font-size: 20px;
            font-weight: bold;
            margin: 0 0 8px;
            color: #0f172a;
        }

        .subtitle {
            margin: 0 0 16px;
            color: #475569;
        }

        .card {
            background: #f8fafc;
            border: 1px solid #e2e8f0;
            border-radius: 6px;
            padding: 16px;
            margin: 16px 0;
        }

        .btn {
            display: inline-block;
            background: #0ea5e9;
            color: #ffffff;
            text-decoration: none;
            padding: 12px 18px;
            border-radius: 6px;
            font-weight: bold;
        }

        .btn:hover {
            background: #0284c7;
        }

        .muted {
            color: #64748b;
            font-size: 13px;
        }

        .divider {
            height: 1px;
            background: #e5e7eb;
            margin: 20px 0;
        }

        .footer {
            padding: 16px 20px 24px;
            text-align: center;
            font-family: Arial, Helvetica, sans-serif;
            color: #6b7280;
            font-size: 12px;
        }

        @media (prefers-color-scheme: dark) {
            body {
                background: #0b1220;
            }

            .container {
                background: #0f172a;
                box-shadow: none;
            }

            .header {
                background: #0b766e;
            }

            .brand-title {
                color: #c1f5ee;
            }

            .content {
                color: #e5e7eb;
            }

            .title {
                color: #ffffff;
            }

            .subtitle, .muted {
                color: #a3a3a3;
            }

            .card {
                background: #111827;
                border-color: #1f2937;
            }

            .divider {
                background: #1f2937;
            }

            .footer {
                color: #9ca3af;
            }
        }
    </style>
</head>
<body>
<div class="wrapper">
    <div class="container">
        <div class="header">
            <a class="brand" href="{{ appUrl }}" target="_blank" rel="noopener">
                <img src="{{ logoUrl }}" alt="Green Googies">
            </a>
        </div>

        <div class="content">
            <h1 class="title">Confirmez votre adresse e‑mail</h1>
            <p class="subtitle">
                Merci pour votre inscription. Pour activer votre compte, veuillez confirmer votre adresse e‑mail.
            </p>

            <div class="card">
                <p style="margin-top:0;">Cliquez sur le bouton ci‑dessous pour valider votre adresse :</p>
                <p style="margin:16px 0;">
                    <a class="btn" href="{{ signedUrl }}" target="_blank" rel="noopener">Confirmer mon e‑mail</a>
                </p>
                <p class="muted" style="margin-bottom:0;">
                    Ce lien expirera
                    {% if expiresAtMessageKey is defined %}
                        {{ expiresAtMessageKey|trans(expiresAtMessageData, 'VerifyEmailBundle') }}
                    {% else %}
                        prochainement pour des raisons de sécurité.
                    {% endif %}
                </p>
            </div>

            <div class="divider"></div>

            <p class="muted">
                Si le bouton ne fonctionne pas, copiez et collez ce lien dans votre navigateur :
                <br>
                <a href="{{ signedUrl }}" target="_blank"
                   style="color:#0ea5e9; word-break:break-all;">{{ signedUrl }}</a>
            </p>

            <p style="margin-top:20px;">
                À bientôt,<br>
                L’équipe Green Goodies
            </p>
        </div>

        <div class="footer">
            Cet e‑mail a été envoyé automatiquement, merci de ne pas y répondre.
            <br>
            Besoin d’aide ? Visitez notre site&nbsp;: <a href="{{ appUrl }}" target="_blank"
                                                         style="color:#0ea5e9; text-decoration:none;">{{ appUrl }}</a>
        </div>
    </div>
</div>
</body>
</html>
